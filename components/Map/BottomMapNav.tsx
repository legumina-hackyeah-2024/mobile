import {StyleSheet, View} from "react-native";

interface BottomMapNavProps {
    nextStation: string
    theme: string
    duration: number
    distanceLeft: number
}

export function BottomMapNav({nextStation, theme, distanceLeft, duration}: BottomMapNavProps) {
    return <View>

    </View>
}

const styles = StyleSheet.create({

})